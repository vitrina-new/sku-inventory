spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true

logging:
  level:
    root: WARN
    com.retailer.sku: INFO

# Disable OpenTelemetry for tests
otel:
  sdk:
    disabled: true
